package org.characters.classes;

import org.characters.Character;

public class Dwarf extends Character {
    public Dwarf(String name, int smallestHealthPoints, int largestHealthPoints) {
        super(name, smallestHealthPoints, largestHealthPoints);
    }
}
